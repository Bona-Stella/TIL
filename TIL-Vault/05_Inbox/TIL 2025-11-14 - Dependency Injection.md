---
created: 2025-11-14 23:21
updated: 2025-11-14 23:21
status:
  - draft
tags:
  - DI
source:
---
# 📝 TIL 2025-11-14 - Dependency Injection

> **관련 주제:** [[MOC_TBD]] 

## 🚀 개요 및 핵심 (Summary)

DI는 Spring 컨테이너가 의존 객체를 주입해 주는 과정이며, 개발자는 주로 클래스 내에서 주입받을 위치에 따라 세 가지 방법을 사용
- 필드 주입
- 수정자 주입
- 생성자 주입

---

## 📚 상세 내용 (Deep Dive)

### 1. 💉 필드 주입 (Field Injection)

- **방식:** 필드에 직접 `@Autowired`를 붙여 주입합니다.
    
- **특징 및 문제점:**
    
    - **간결성:** 코드가 가장 간결합니다.
        
    - **강한 결합/테스트 어려움 (❌):** 외부에서 의존 객체를 주입할 방법이 없으므로, **테스트 코드 작성 시** Spring 컨테이너 없이는 테스트가 불가능합니다. 이는 곧 **DI 원칙을 위반**하고 코드의 유연성을 해칩니다.
        
    - **불변성 확보 불가:** 필드를 `final`로 선언할 수 없으므로, 객체의 불변성을 보장할 수 없습니다.
        

### 2. ✏️ 수정자 주입 (Setter Injection)

- **방식:** Setter 메서드에 `@Autowired`를 붙여 주입합니다.
    
- **특징:**
    
    - **선택적 주입 가능:** 주입할 의존 객체가 필수가 아닐 경우, `@Autowired(required = false)`를 사용하여 주입하지 않을 수도 있습니다.
        
    - **변경 가능성:** 메서드가 공개(public)되어 있어 런타임에 언제든지 의존 객체를 **변경**할 수 있습니다.
        
    - **불변성 확보 불가:** 마찬가지로 필드를 `final`로 선언할 수 없습니다.
        

### 3. 🏗️ 생성자 주입 (Constructor Injection)

- **방식:** 클래스의 **생성자**에 `@Autowired`를 붙여 주입합니다. (Spring Boot 환경에서는 `@Autowired`를 생략하는 것이 일반적입니다.)
    
- **특징 및 장점 (✅):**
    
    - **불변성 보장:** 주입받는 필드를 **`final`**로 선언할 수 있어, 객체가 생성된 후에는 의존 관계가 **변하지 않음(불변성)**을 보장합니다. **가장 권장되는 이유**입니다.
        
    - **누락 검증 (컴파일 시 에러 체크):** 필수 의존 객체가 누락되었을 경우, 컴파일러가 아니라 **Spring 컨테이너 구동 시점**에 누락 여부를 확인하고 에러를 발생시켜 (Setter/Field 주입보다) **문제 발견 시점이 빠릅니다.**
        
    - **테스트 용이성:** 생성자를 통해 일반 자바 코드처럼 의존 객체를 주입할 수 있어 **순수 Java 환경에서 단위 테스트**가 매우 쉽습니다.
        

---

## 💡 권장되는 DI 방식

|**방식**|**권장 여부**|**이유**|
|---|---|---|
|**생성자 주입**|**가장 권장**|**`final` 키워드**를 통한 **불변성** 및 **테스트 용이성** 확보|
|**수정자 주입**|선택적 주입 시에만 제한적 사용|의존 객체가 필수값이 아니고, 변경 가능해야 할 때|
|**필드 주입**|**권장하지 않음**|불변성 확보 불가, 테스트 어려움, DI 원칙 위반|


---
created: 2025-11-12 16:13
updated: 2025-11-12 16:13
status:
  - draft
tags:
  - Array
source: Programmers
---
# 📝 TIL 2025-11-12 - 숫자의 표현(연속된 자연수 합)

> **관련 주제:** [[MOC_TBD]] 

## 🚀 개요 및 핵심 (Summary)

## **문제 설명**

Finn은 요즘 수학공부에 빠져 있습니다. 수학 공부를 하던 Finn은 자연수 n을 연속한 자연수들로 표현 하는 방법이 여러개라는 사실을 알게 되었습니다. 예를들어 15는 다음과 같이 4가지로 표현 할 수 있습니다.

- 1 + 2 + 3 + 4 + 5 = 15
- 4 + 5 + 6 = 15
- 7 + 8 = 15
- 15 = 15

자연수 n이 매개변수로 주어질 때, 연속된 자연수들로 n을 표현하는 방법의 수를 return하는 solution를 완성해주세요.

## **제한사항**

- n은 10,000 이하의 자연수 입니다.

---

## **입출력 예**

|n|result|
|---|---|
|15|4|

---

## 📚 상세 내용 (Deep Dive)
> **코딩 오류 및 피드백**
> x+y의 정확한 길이 체크. 마지막 두 수의 합은 `length - 1` + `length`
```java
public class Main {
    public int result(int n) {
        int sum = 0, count = 1;

        int[] nArray = new int[n / 2 + 1];
        for (int i = 0; i < nArray.length; i++) {
            nArray[i] = i + 1;
        }

        for (int lt = 0; lt < nArray.length - 1; lt++) {
            for (int rt = lt; rt < nArray.length; rt++) {
                sum += nArray[rt];
                if (sum > n) {
                    sum = 0;
                    break;
                }
                if (sum == n) {
                    sum = 0;
                    count++;
                    break;
                }
            }
        }

        return count;
    }

    public static void main(String[] args) {
        Main T = new Main();
        int n = 20;
        System.out.println(T.result(n));
    }
}
```
 >**AI 개선안**
 >투 포인터(슬라이딩 윈도우) — **시간 O(n), 공간 O(1)**
 >-> 연속 구간 합이므로 윈도우 `[lt..rt]`의 합을 유지하면서 `n`과 비교
 >수론(재배열 불등식이 아니라 **홀수 약수 개수** 정리) — **시간 O(√n), 공간 O(1)**
```java
// AI 피드백 1
public int result(int n) {
    // 길이 1(=n 자체) 케이스를 미리 포함
    int count = 1;

    int lt = 1, rt = 1, sum = 1;
    // lt가 n/2를 넘어가면 길이 ≥2의 연속합으로는 더 이상 n을 만들 수 없음
    while (lt <= n / 2) {
        if (sum < n) {
            rt++;
            sum += rt;
        } else if (sum > n) {
            sum -= lt;
            lt++;
        } else { // sum == n
            count++;
            sum -= lt;
            lt++;
        }
    }
    return count;
}

// AI 피드백 2
public int result(int n) {
    int count = 0;
    for (int d = 1; d * d <= n; d++) {
        if (n % d == 0) {
            int e = n / d;
            // d가 홀수 약수
            if ((d & 1) == 1) count++;
            // 서로 다른 짝의 e도 홀수면 추가
            if (e != d && (e & 1) == 1) count++; 
        }
    }
    return count;
}
```
```java
// 다른 사람 풀이
public class Expressions {

    public int expressions(int num) {
        int answer = 0;
        for (int i = 1; i <= num; i += 2) {
            if (num % i == 0) {
                answer++;
            }
        }
        return answer;
    }

    public static void main(String args[]) {
        Expressions expressions = new Expressions();
        System.out.println(expressions.expressions(15));
    }
}
```
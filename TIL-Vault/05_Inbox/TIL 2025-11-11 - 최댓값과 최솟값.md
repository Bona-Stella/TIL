---
created: 2025-11-11 21:21
updated: 2025-11-11 21:21
status:
  - draft
tags:
  - Array
source: Programmers
---
# 📝 TIL 2025-11-11 - 최댓값과 최솟값

> **관련 주제:** [[MOC_TBD]] 

## 🚀 개요 및 핵심 (Summary)

## **문제 설명**

문자열 s에는 공백으로 구분된 숫자들이 저장되어 있습니다. str에 나타나는 숫자 중 최소값과 최대값을 찾아 이를 "(최소값) (최대값)"형태의 문자열을 반환하는 함수, solution을 완성하세요.  
예를들어 s가 "1 2 3 4"라면 "1 4"를 리턴하고, "-1 -2 -3 -4"라면 "-4 -1"을 리턴하면 됩니다.

## **제한 조건**

- s에는 둘 이상의 정수가 공백으로 구분되어 있습니다.

## **입출력 예**

| s             | return  |
| ------------- | ------- |
| "1 2 3 4"     | "1 4"   |
| "-1 -2 -3 -4" | "-4 -1" |
| "-1 -1"       | "-1 -1" |

---

## 📚 상세 내용 (Deep Dive)
> **코딩 오류 및 피드백**
> Array로 변환하는 법이 어색함
```java
// 제출 답안
import java.util.Scanner;  
  
public class Main {  
    public String solution(String str) {  
        String[] s = str.split(" ");  
  
        int[] ints = new int[s.length];  
        for (int i = 0; i < s.length; i++) {  
            ints[i] = Integer.parseInt(s[i]);  
        }  
        int max = Integer.MIN_VALUE;  
        int min = Integer.MAX_VALUE;  
  
        for (int anInt : ints) {  
            if (anInt > max) max = anInt;  
            if (anInt < min) min = anInt;  
        }  
        return max + " " + min;  
    }  
  
    public static void main(String[] args) {  
        Main main = new Main();  
        Scanner scanner = new Scanner(System.in);  
        String str = scanner.nextLine();  
        System.out.println(main.solution(str));  
    }  
}
```

> **AI 개선안**
> 중복 루프를 제거하고, 문자열을 숫자로 변환하면서 동시에 min/max를 계산할 수 있음
> **스트림 API 활용 (Java 8+)**: 간결하게 작성하려면 다음처럼 가능
```java
// AI 피드백 1
public String solution(String str) {
    String[] s = str.split(" ");

    int max = Integer.MIN_VALUE;
    int min = Integer.MAX_VALUE;

    for (String numStr : s) {
        int num = Integer.parseInt(numStr);
        if (num > max) max = num;
        if (num < min) min = num;
    }

    return max + " " + min;
}

// AI 피드백 2
public String solution(String str) {
    IntSummaryStatistics stats = Arrays.stream(str.split(" "))
        .mapToInt(Integer::parseInt)
        .summaryStatistics();
    return stats.getMax() + " " + stats.getMin();
}
```
